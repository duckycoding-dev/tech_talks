---
import RootLayout from '@/layouts/RootLayout.astro';
---

<RootLayout title="Bonus: Prefetch Strategies">
  <h1>Prefetch Strategies</h1>
  <p class="intro">
    Astro's <code>data-astro-prefetch</code> attribute gives you granular
    control over when to load the next page's JavaScript and assets. Open your
    Network tab to see the magic happen!
  </p>

  <div class="grid">
    <!-- 1. Hover (Default) -->
    <div class="card strategy-card">
      <div class="icon">🖱️</div>
      <h2>Hover</h2>
      <p>Fetches when you mouse over the link.</p>
      <p>Mostly useful for desktop users.</p>
      <div class="action"> <a
        href="/demos/1-basics"
        data-astro-prefetch="hover"
        class="btn"
        >/demos/1-basics</a
      > </div>
    </div>

    <!-- 2. Tap (Mobile Friendly) -->
    <div class="card strategy-card">
      <div class="icon">👆</div>
      <h2>Tap</h2>
      <p>Fetches immediately on touchstart/mousedown (faster than click).</p>
      <p>Mostly useful for mobile / touch interfaces users.</p>
      <div class="action"> <a
        href="/demos/2-routing"
        data-astro-prefetch="tap"
        class="btn"
        >/demos/2-routing</a
      > </div>
    </div>

    <!-- 3. Load (Aggressive) -->
    <div class="card strategy-card">
      <div class="icon">⚡</div>
      <h2>Load</h2>
      <p>Fetches as soon as this page loads. Use sparingly! </p>
      <div class="action"> <a
        href="/demos/3-islands"
        data-astro-prefetch="load"
        class="btn"
        >/demos/3-islands</a
      > </div>
    </div>

    <!-- 4. External link (No Prefetch) -->
    <div class="card strategy-card">
      <div class="icon">🦆</div>
      <h2>External</h2>
      <p>Prefetching is disabled for external links.</p>
      <div class="action"> <a
        href="https://duckycoding.dev"
        class="btn"
        >duckycoding.dev</a
      > </div>
    </div>
  </div>

  <div class="scroll-down-cta">
    <p>⬇️ Scroll down for Viewport Prefetch ⬇️</p>
    <div class="arrow">↓</div>
  </div>
  <div class="spacer"
    ><p>🥱</p></div
  >

  <!-- 5. Viewport (Lazy) -->
  <div class="card strategy-card viewport-card">
    <div class="icon">👀</div>
    <h2>Viewport</h2>
    <p>Fetches only when this link enters your screen.</p>
    <div class="action"> <a
      href="/demos/4-server"
      data-astro-prefetch="viewport"
      class="btn"
      >/demos/4-server</a
    > </div>
  </div>
</RootLayout>

<style>
  .intro {
    text-align: center;
    max-width: 650px;
    margin: 0 auto 3rem auto;
    color: var(--color-text);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .strategy-card {
    display: flex;
    flex-direction: column;
    background: var(--color-card);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 2rem;
    text-align: center;
    transition: transform 0.2s;
  }

  .action {
    margin-top: auto;
  }

  .strategy-card:hover {
    transform: translateY(-5px);
    border-color: var(--color-link);
  }

  .icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  h2 {
    color: var(--color-heading);
    margin-bottom: 0.5rem;
  }

  p {
    margin-bottom: 1.5rem;
    font-size: 0.95rem;
  }

  .btn {
    display: inline-block;
    background: var(--color-link);
    color: var(--space-black);
    padding: 0.8rem 1.5rem;
    border-radius: 6px;
    font-weight: bold;
    text-decoration: none;
    width: 100%;
  }

  .btn:hover {
    opacity: 0.9;
  }

  .scroll-down-cta {
    text-align: center;
    margin-top: 2rem;
    color: var(--color-text);
  }

  .spacer {
    p {
      font-size: 5rem;
    }

    height: 150vh; /* Make it really tall */
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    color: #888;
    border-left: 2px dashed rgba(255, 255, 255, 0.1);
    margin: 0 auto;
  }

  .arrow {
    font-size: 2rem;
    animation: bounce 2s infinite;
  }

  .viewport-card {
    max-width: 400px;
    margin: 0 auto 4rem auto;
    border-color: var(--accent);
    box-shadow: 0 0 20px rgba(56, 189, 248, 0.2);
  }

  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }
</style>
