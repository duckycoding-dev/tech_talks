---
import RootLayout from '@/layouts/RootLayout.astro';
import ReactCounter from '@/components/react-components/Counter';
import VueCounter from '@/components/vue-components/Counter.vue';
import SvelteCounter from '@/components/svelte-components/Counter.svelte';
import SolidCounter from '@/components/solid-components/Counter';
import Counter from '@/components/Counter.astro';
---

<RootLayout title="3. Islands Architecture">
  <h1>Astro Islands</h1>
  <p class="subtitle"
    >Multi-Framework, Hydration Strategies & Zero JS by Default</p
  >

  <div class="grid">
    <!-- 1. Static (Server Only) -->
    <div class="card">
      <div class="badge server">Server Only</div>
      <h2>Static HTML</h2>
      <p>Rendered on the server. No JS sent to client.</p>
      <div class="demo-wrapper">
        <Counter />
      </div>
    </div>

    <!-- 2. Client:load (Hydrate Immediately) -->
    <div class="card">
      <div class="badge client">client:load</div>
      <h2>Immediate Hydration</h2>
      <p>Hydrates as soon as the JavaScript bundle loads.</p>
      <div class="demo-wrapper">
        <VueCounter client:load />
      </div>
    </div>

    <!-- 3. Client:idle (Hydrate when idle) -->
    <div class="card">
      <div class="badge client">client:idle</div>
      <h2>Idle Hydration</h2>
      <p>Hydrates when the main thread is free (requestIdleCallback).</p>
      <div class="demo-wrapper">
        <SvelteCounter
          client:idle
          title="client:idle"
        />
      </div>
    </div>

    <!-- 4. Client:media (Hydrate on query) -->
    <div class="card">
      <div class="badge client">client:media</div>
      <h2>Media Query</h2>
      <p>Hydrates only on larger screens (min-width: 1000px).</p>
      <div class="demo-wrapper">
        <ReactCounter client:media="(min-width: 1000px)" />
      </div>
    </div>

    <!-- 5. Client:only (Client Only) -->
    <div class="card">
      <div class="badge client">client:only</div>
      <h2>Client Only</h2>
      <p
        >Skips SSR entirely. Useful for libraries accessing window/localStorage.</p
      >
      <div class="demo-wrapper">
        <SvelteCounter
          client:only="svelte"
          title="client:only"
          ><p slot="fallback">Fallback while loading ⏳⏳⏳</p></SvelteCounter
        >
      </div>
    </div>
  </div>

  <div class="info-box">
    <h3>💡 Framework Agnostic</h3>
    <p
      >Notice how we are mixing React, Vue, Svelte, and SolidJS on the same
      page? That's the power of Astro Islands.</p
    >
    <p
      >Check the Network tab to see different bundles loading at different
      times!</p
    >
  </div>

  <div class="scroll-down-cta">
    <p>⬇️ Scroll down for Viewport Prefetch ⬇️</p>
    <div class="arrow">↓</div>
  </div>
  <div class="spacer"
    ><p>🥱</p></div
  >

  <!-- 6. Client:visible (Hydrate when visible) -->
  <div class="card">
    <div class="badge client">client:visible</div>
    <h2>Visible Hydration</h2>
    <p>Scroll down! Hydrates only when it enters the viewport.</p>
    <div class="demo-wrapper">
      <SolidCounter client:visible />
    </div>
  </div>
</RootLayout>

<script is:inline>
  // Simulate a heavy task blocking the main thread for 2 seconds
  // This demonstrates how client:idle waits until the thread is free
  const start = Date.now();
  console.log('🔴 BLOCKING MAIN THREAD (2s)...');
  while (Date.now() - start < 2000) {
    // Busy wait
  }
  console.log('🟢 MAIN THREAD RELEASED');
</script>

<style>
  .scroll-down-cta {
    text-align: center;
    margin-top: 2rem;
    color: var(--color-text);
  }

  .spacer {
    p {
      font-size: 5rem;
    }

    height: 150vh; /* Make it really tall */
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    color: #888;
    border-left: 2px dashed rgba(255, 255, 255, 0.1);
    margin: 0 auto;
  }

  .arrow {
    font-size: 2rem;
    animation: bounce 2s infinite;
  }
  .subtitle {
    text-align: center;
    color: var(--color-text);
    margin-bottom: 3rem;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .card {
    background: var(--color-card);
    border-radius: 12px;
    padding: 1.5rem;
    position: relative;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    padding: 0.25rem 0.75rem;
    border-radius: 999px;
    font-size: 0.75rem;
    font-weight: bold;
    text-transform: uppercase;
  }

  .badge.server {
    background: #333;
    color: #fff;
  }
  .badge.client {
    background: var(--accent);
    color: #000;
  }

  h2 {
    font-size: 1.25rem;
    color: var(--color-heading);
    margin-bottom: 0.5rem;
  }
  p {
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
    color: #a1a1aa;
  }

  .demo-wrapper {
    background: rgba(0, 0, 0, 0.3);
    padding: 1rem;
    border-radius: 8px;
  }

  .info-box {
    background: rgba(65, 184, 131, 0.1);
    border: 1px dashed #41b883;
    padding: 2rem;
    border-radius: 8px;
    text-align: center;
  }
  .info-box h3 {
    color: #41b883;
  }
</style>
