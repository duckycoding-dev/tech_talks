---
const serverTime = new Date().toLocaleTimeString('en-US', {
  timeZone: 'UTC',
  hour: '2-digit',
  minute: '2-digit',
  second: '2-digit',
});
const serverDate = new Date().toLocaleDateString('en-US', {
  timeZone: 'UTC',
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

// For demonstration purposes, let's also log the server time to the console
console.log(`📅 Server Date: ${serverDate}`);
console.log(`🕒 Server Time (UTC): ${serverTime}`);
---

<div class="terminal">
  <div class="terminal-header">
    <span class="dot red"></span>
    <span class="dot yellow"></span>
    <span class="dot green"></span>
    <span class="terminal-title">server-output.html</span>
  </div>
  <div class="terminal-body">
    <p class="line"
      ><span class="prompt">$</span> Server Date:
      <span class="value">{serverDate}</span></p
    >
    <p class="line"
      ><span class="prompt">$</span> Server Time (UTC):
      <span class="value">{serverTime}</span></p
    >
    <p class="line"
      ><span class="prompt">$</span> Rendered on:
      <span class="value">Server</span></p
    >
    <p class="line"
      ><span class="prompt">$</span> JavaScript shipped:
      <span class="value zero">0 bytes</span></p
    >
  </div>
</div>

<p class="hint"
  >View the page source — this section is pure static HTML. No
  <code>&lt;script&gt;</code> tag, no hydration, no runtime.</p
>

<style>
  .terminal {
    background: #0d1117;
    border: 1px solid #30363d;
    border-radius: 8px;
    overflow: hidden;
    font-family: 'Courier New', monospace;
  }

  .terminal-header {
    background: #161b22;
    padding: 0.5rem 0.75rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .dot.red {
    background: #ff5f57;
  }
  .dot.yellow {
    background: #febc2e;
  }
  .dot.green {
    background: #28c840;
  }

  .terminal-title {
    margin-left: 0.5rem;
    font-size: 0.75rem;
    color: #8b949e;
  }

  .terminal-body {
    padding: 1rem;
  }

  .line {
    margin: 0.3rem 0;
    color: #c9d1d9;
    font-size: 0.9rem;
  }

  .prompt {
    color: #58a6ff;
    margin-right: 0.5rem;
  }

  .value {
    color: #7ee787;
    font-weight: bold;
  }

  .value.zero {
    color: #ffa657;
  }

  .hint {
    margin-top: 1rem;
    font-size: 0.85rem;
    color: #8b949e;
    font-style: italic;
  }

  .hint code {
    background: rgba(56, 189, 248, 0.1);
    padding: 0.1rem 0.3rem;
    border-radius: 3px;
    color: var(--accent);
    font-style: normal;
  }
</style>
